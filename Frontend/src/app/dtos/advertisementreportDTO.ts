import {User} from '../model/user';
import {CommentPreviewDTO} from './comment-preview-dto';

export class AdvertisementreportDTO {
  id: number;
  name: string;
  model: string;
  brand: string;
  travelled: number;
  bookingID: number;
}
